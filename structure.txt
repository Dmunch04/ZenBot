# This is how the imports should be ordered #
-- First package imports
import os
import eve
import shutil

-- Then discord imports
import discord
from discord.ext import commands

-- Then repo script imports
import Config
from Modules import RoleCheck as role
from Helpers import EmbedHelper as embed



# This is how the ctx should be ordered #
Server = ctx.message.server
Channel = ctx.message.channel
Sender = ctx.message.author
Message = ctx.message
